spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: #####
    password: ####
    url: jdbc:mysql://#########:8080/lifen_project?characterEncoding=utf-8&useSSL=false
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  jackson:
    default-property-inclusion: non_null
  thymeleaf:
    mode: HTML5
    cache: false
server:
  context-path: /lifen
  port: 8001

